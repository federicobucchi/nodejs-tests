Remember

1) Open a terminal window and keep executed mongod (MongoDB server running):
$ mongod

2) For test on your localhost db connect using the mongo client:
$ mongo

Files

- install.js > create collection and insert data
- reset.js > delete all collections and data
- app.js > methods
- config.js > config vars